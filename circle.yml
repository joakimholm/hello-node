machine:
  services:
    - docker
  node:
    version: 6.1.0
dependencies:
  override:
    - docker-compose -p ci build
    - docker-compose -p ci up -d
test:
  override:
    - echo "Running tests..."
